<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.firebaseproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SongDetail"></activity>
        <activity android:name=".MusicMain" />
        <activity android:name=".memo2" />
        <activity android:name=".MainMemo" />
        <activity android:name=".gmresult" />
        <activity android:name=".gmstart" />
        <activity android:name=".gmexplain" />
        <activity android:name=".btn1" />
        <activity android:name=".AppFirstActivity" />
        <activity android:name=".boardActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>